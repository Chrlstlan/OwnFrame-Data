{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OwnFrame ducat values",
  "description": "Validates data/ducats.json: Baro Ki'Teer ducat sale value per Prime part. This value appears in no official dataset and not on warframe.market — it is curated from in-game (relic refinement view). _-prefixed keys are file metadata, not data entries.",
  "type": "object",
  "properties": {
    "_comment": { "type": "string" },
    "_example": { "type": "object" },
    "_date": { "type": "string" }
  },
  "patternProperties": {
    "^(?!_)": {
      "type": "integer",
      "minimum": 0,
      "maximum": 1000
    }
  },
  "additionalProperties": false
}